## Settings file was created by plugin Manimal-Icebreaker v0.2.3
## Plugin GUID: com.manimal.icebreaker

[Experimental]

## EXPERIMENT: at raid load, replace the map's baked cover points with scanner-generated ones. adds ~10-30s to load. bots then use OUR points — for validating the custom-map baker
# Setting type: Boolean
# Default value: true
InjectGeneratedCovers = true

[General]

## dump the ai data right after the game restores it at raid load (icebreaker only — dev diagnostic)
# Setting type: Boolean
# Default value: false
AutoDumpOnRaidLoad = false

## re-dump on demand mid-raid (icebreaker only; F9 is the fog tuner — bind something else)
# Setting type: KeyboardShortcut
# Default value: None
DumpHotkey = 

## run the prototype cover scanner on the current map and dump the result — freezes the game for a bit (icebreaker only)
# Setting type: KeyboardShortcut
# Default value: None
GenerateHotkey = 

## pretty-print the json — files get roughly 3x bigger
# Setting type: Boolean
# Default value: true
IndentJson = true

[Icebreaker]

## brightness of the revived interior lamp lights
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0 to 12
LampIntensity = 3

## flat ambient fill light — lifts shadowed areas out of black (no real bounce without a bake)
# Setting type: Single
# Default value: 0.8
# Acceptable value range: From 0 to 3
AmbientIntensity = 0.8

## let the revived lamps cast realtime shadows — much prettier, much heavier (1500+ lights)
# Setting type: Boolean
# Default value: false
LampShadows = false

## distance-culling aggressiveness for small props (lower = culls closer = more fps) (live)
# Setting type: Single
# Default value: 0.5
# Acceptable value range: From 0.25 to 3
CullDistanceScale = 0.5

## occlusion culling driver — flip OFF (live) to un-cull everything; the pop-in isolation tool: if pops stop with this off, the bake's sightline data is the culprit
# Setting type: Boolean
# Default value: true
PcDriverEnabled = true

## EXPERIMENTAL: run BSG's own 84k-cell retail culling bake instead of our 6 sidecar volumes. needs the 231MB 065281ec..._packed_cull.bytes in EscapeFromTarkov_Data\StreamingAssets\Culling_Data (NOT shipped in the mod zip) — silently falls back to the sidecars when the file is missing. much finer room-vs-room isolation indoors; tested slower overall in July (outdoor-heavy), re-trialing for interiors. needs a raid restart
# Setting type: Boolean
# Default value: false
NativeCulling = false

## override unity's LOD bias on this map (live). THE dense-view fps fix (08-08 FrameSplit hunt) — but NOT for the reason the name suggests: 97.5% of this map's 82k LODGroups are single-LOD, so bias here scales how far out props CULL, not mesh detail. lower = props cull nearer = fps; the LodCullFloor cap keeps near-cullers (furniture) from popping in plain sight. 0.7 + floor 0.01 is the tested sweet spot; -1 = hands off (the game's 2.0 = retail distances)
# Setting type: Single
# Default value: 0.7
# Acceptable value range: From -1 to 2
LodBiasClamp = 0.7

## FAR-tier cull cap (LIVE): beyond the near radius, props cull when smaller than this screen fraction. the proven fps lever — higher = earlier culls = more fps + more distant pop; -1 = retail cull heights. pairs with the near tier so aggression out here never touches whats in your face
# Setting type: Single
# Default value: 0.1
# Acceptable value range: From -1 to 0.1
LodCullFloor = 0.1

## NEAR-tier cull cap (LIVE): inside LodCullNearRadius, props only vanish below this screen fraction — the anti-dither guarantee for furniture around you. -1 = retail heights near you
# Setting type: Single
# Default value: 0.006
# Acceptable value range: From -1 to 0.05
LodCullNearFloor = 0.006

## meters around the camera that count as the near tier while OUTDOORS (LIVE). cells re-tier when you cross a cell boundary
# Setting type: Single
# Default value: 26
# Acceptable value range: From 5 to 100
LodCullNearRadius = 26

## near-tier radius while INDOORS (LIVE). corridor sightlines are short — a tighter bubble lets the far tier eat the rest of the deck
# Setting type: Single
# Default value: 19.49
# Acceptable value range: From 5 to 100
LodCullNearRadiusIndoor = 19.49

## force-skip LOD0 globally (live diagnostic). 1 = every LODGroup renders LOD1 or lower — brutal visually but if fps rockets, the dense-view cost is LOD0 mesh detail and lod bias tuning is the shipping fix. -1 = off
# Setting type: Int32
# Default value: -1
# Acceptable values: -1, 0, 1, 2
MaxLodClamp = -1

## enforce the retail shadow split at load: _SHADOW_ proxy meshes cast-only, their visual siblings cast nothing (double-cast shadows otherwise)
# Setting type: Boolean
# Default value: true
ShadowProxyFix = true

## pre-spawn the trigger squads into an off-map pen during the early raid and TELEPORT them in when events fire. off (default) = profiles premade + bundles prewarmed, bots spawned on demand through the real pipeline
# Setting type: Boolean
# Default value: false
CrewPreSpawnPool = false

## EXPERIMENTAL: spawn the trigger squads (BD/knight/wedges) through BSG's own BossLocationSpawn botEvent pipeline with the retail 1.0 wave table, instead of our force-spawner. retail-exact squad sizes and grouping; known tradeoffs: spawn-moment generation hitch (no premake), boss-group follower quirks. needs a raid restart
# Setting type: Boolean
# Default value: false
NativeBdWaves = false

## spawn solo Knight among the rogues
# Setting type: Boolean
# Default value: true
CrewKnight = true

## spawn Black Division squads when the start-cutscene trigger is hit (needs the BlackDiv mod)
# Setting type: Boolean
# Default value: true
CrewBlackDivision = true

## resurrect BSG's spatial audio (room/portal occlusion) from the recovered retail bake — needs the acoustics sidecar next to the dll
# Setting type: Boolean
# Default value: true
SpatialAudio = true

## restore the ~1100 retail lens flares (lamp glow sprites). OFF is a perf A/B lever — over a thousand flare components have measurable render cost. needs a raid restart
# Setting type: Boolean
# Default value: true
LensFlares = true

## rebuild the retail indoor/outdoor switcher volumes (indoor sound banks, exposure, rain muffling)
# Setting type: Boolean
# Default value: true
EnvironmentTriggers = true

## gain multiplier for door open/close/squeak foley (ripped clips are mixed quiet; play volume clamps at 1.0)
# Setting type: Single
# Default value: 4
# Acceptable value range: From 0.5 to 4
DoorSoundBoost = 4

## OUR hand-placed ambient beds (the Amb_<zone> room tones + outdoor wind bed from the Author 8 pass). off by default now that retail's own per-room tones are restored onto the SpatialAudioRooms — leaving both on stacks two room tones. turn it back ON if the authored tones fail to bind (watch the '[Acoustics] bound N authored room tones' line) (live)
# Setting type: Boolean
# Default value: false
AmbientBeds = false

## how much of the outdoor wind bleeds through indoors (live; 0 = silent inside, 1 = full)
# Setting type: Single
# Default value: 0.054
# Acceptable value range: From 0 to 1
WindIndoorFraction = 0.054

## resurrect BSG's weather/sky stack (clouds, wind, rain/snow, time-of-day) from the recovered retail components (needs the Author 9 weather-asset bundle)
# Setting type: Boolean
# Default value: true
WeatherSystem = true

## force Winter season on icebreaker only (snowfall; other maps keep the server's season; needs WeatherSystem)
# Setting type: Boolean
# Default value: true
ForceWinter = true

## permanent blizzard: pins WeatherDebug (snow/wind/fog) + raises the winter STORM state; also pins the TOD hour (live)
# Setting type: Boolean
# Default value: true
Blizzard = true

## comma-separated component type names the donor graft must skip (bisecting a bad graft component)
# Setting type: String
# Default value: 
CamDonorSkip = 

## developer tooling: on-screen tuners, scene probes, dump hotkeys and the diagnostic components. OFF for normal play — this does not affect logging, set LogLevels in BepInEx.cfg for that
# Setting type: Boolean
# Default value: false
DevMode = false

## enable the F1-F12 diagnostic hotkey suite (lights toggle, geo hide, batching tests, probes). OFF for normal play — F9 doubles as the fog tuner toggle and MUST not fight the lights toggle (live)
# Setting type: Boolean
# Default value: false
DiagHotkeys = false

## play the in-engine helicopter cutscene at the story trigger (falls back to the video if the cutscene scene/timeline is unavailable)
# Setting type: Boolean
# Default value: true
TimelineCutscene = true

## spawn the smugglers' hovercraft transit on the Shoreline coast once the Boreas chain is finished (takes you to the icebreaker)
# Setting type: Boolean
# Default value: true
HovercraftTransit = true

## hovercraft transit position X on Shoreline (live)
# Setting type: Single
# Default value: -312.0199
# Acceptable value range: From -2000 to 2000
TransitX = -312.0199

## hovercraft transit position Y on Shoreline (live)
# Setting type: Single
# Default value: -65.899
# Acceptable value range: From -500 to 500
TransitY = -65.899

## hovercraft transit position Z on Shoreline (live)
# Setting type: Single
# Default value: 527.0115
# Acceptable value range: From -2000 to 2000
TransitZ = 527.0115

## hovercraft rotation X (270 = the OBJ axis correction) (live)
# Setting type: Single
# Default value: 270
# Acceptable value range: From 0 to 360
TransitRotX = 270

## hovercraft rotation Y, its heading (live)
# Setting type: Single
# Default value: 40
# Acceptable value range: From 0 to 360
TransitRotY = 40

## hovercraft rotation Z (live)
# Setting type: Single
# Default value: 0
# Acceptable value range: From 0 to 360
TransitRotZ = 0

## transit trigger position X (live)
# Setting type: Single
# Default value: -310.3848
# Acceptable value range: From -2000 to 2000
TransitZoneX = -310.3848

## transit trigger position Y (live)
# Setting type: Single
# Default value: -63.49909
# Acceptable value range: From -500 to 500
TransitZoneY = -63.49909

## transit trigger position Z (live)
# Setting type: Single
# Default value: 531.0692
# Acceptable value range: From -2000 to 2000
TransitZoneZ = 531.0692

## transit trigger box size X in meters (live)
# Setting type: Single
# Default value: 8
# Acceptable value range: From 0.5 to 60
TransitZoneSizeX = 8

## transit trigger box size Y in meters (live)
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0.5 to 60
TransitZoneSizeY = 3

## transit trigger box size Z in meters (live)
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0.5 to 60
TransitZoneSizeZ = 3

## rouble price shown for the hovercraft crossing (live)
# Setting type: Int32
# Default value: 400000
# Acceptable value range: From 0 to 5000000
TransitCost = 400000

## transit trigger heading, match the craft (live)
# Setting type: Single
# Default value: 40
# Acceptable value range: From 0 to 360
TransitZoneRotY = 40

## wind-driven sheets of blown snow around the player, on top of the native flakes (live)
# Setting type: Boolean
# Default value: true
SnowGusts = true

## euros the pilot charges to board the helicopter exfil, 0 for a free ride (read at raid start)
# Setting type: Int32
# Default value: 2400
# Acceptable value range: From 0 to 1000000
HeliExfilCost = 2400

## seconds before the crossing opens, listed red with a countdown until then (every vanilla transit uses 60)
# Setting type: Int32
# Default value: 30
# Acceptable value range: From 0 to 600
TransitActivateAfterSec = 30

## plant the authored grenade tripwires (manimal_tripwire* markers) at raid start
# Setting type: Boolean
# Default value: true
Tripwires = true

## per-marker chance a tripwire is armed this raid — each marker rolls independently, so the layout differs every raid (in coop the host's roll wins, broadcast by the fika sync addon)
# Setting type: Single
# Default value: 0.5
# Acceptable value range: From 0 to 1
TripwireChance = 0.5

## grenade template planted on the wires (default: Manimal CS gas grenade)
# Setting type: String
# Default value: 6a5d6a5f4ed8c025a0a2cff0
TripwireTpl = 6a5d6a5f4ed8c025a0a2cff0

## cull interior volumes (Indoor_01/02/03) wholesale when the camera is outside them, beyond CrossCullDistance — the ship-center fps fix (live)
# Setting type: Boolean
# Default value: true
InteriorCrossCull = true

## how close an out-of-volume interior group must be to still render (doorway/window sightlines) (live)
# Setting type: Single
# Default value: 20
# Acceptable value range: From 10 to 80
CrossCullDistance = 20

## activate the authored passcode terminals + post-it code notes (needs the Author 9 passcode sidecar next to the dll)
# Setting type: Boolean
# Default value: true
PasscodeTerminals = true

## load BSG's recovered baked AI data (covers/voxels/cores/patrols) instead of runtime generation; off or fill-failure = synthesized graph
# Setting type: Boolean
# Default value: true
RetailAIBake = true

## comma-separated BotZone names to rebuild with GENERATED cover+patrol data while the rest of the map keeps the retail bake (needs RetailAIBake on); empty = all retail
# Setting type: String
# Default value: BotZoneKitchen,BotZoneRoomsThirdKitchen,BotZoneRoomsThird,BotZoneKorrDown1,BotZoneFront,BotZoneFront2
SynthZones = BotZoneKitchen,BotZoneRoomsThirdKitchen,BotZoneRoomsThird,BotZoneKorrDown1,BotZoneFront,BotZoneFront2

[VolumetricFog2]

## the REAL volumetric fog (Volumetric Fog & Mist 2, raymarched) — needs volumetricfog.bundle next to the plugin dll (live)
# Setting type: Boolean
# Default value: true
Enabled = true

