## Settings file was created by plugin ORBIT v1.2.1
## Plugin GUID: com.chazut.orbit

[01. Essentials]

## ON (default): when a squadmate takes fire, the others break off and converge to support (ORBIT routes them in, SAIN fights). OFF: everyone fights their own fight.
# Setting type: Boolean
# Default value: true
Squad rally = true

## ON (default): clean log — only warnings & errors, regardless of the Log levels below. Turn it OFF to use the Log levels (e.g. tick Debug there before sending a bug report).
# Setting type: Boolean
# Default value: true
Quiet logging = true

## Which message levels ORBIT writes (used when Quiet logging is OFF). Default: everything except Debug. Tick Debug for a detailed bug-report log — it works in the release build now, not just debug builds.
# Setting type: OrbitLogLevel
# Default value: Info, Warning, Error
# Acceptable values: None, Info, Debug, Warning, Error
# Multiple values can be set at the same time by separating them with , (e.g. Debug, Warning)
Log levels = Info, Warning, Error

## ON: writes a one-line 'PERF' summary (fps, hitches, GC, ORBIT activity counters) to the log every 30s, regardless of the other logging settings. Turn it on before recording a raid for a performance report.
# Setting type: Boolean
# Default value: false
Performance logging = false

## Which factions loot containers, loose items and corpses. Only the listed factions are affected.
# Setting type: LootingFaction
# Default value: Scav, Pmc, PlayerScav
# Acceptable values: None, Scav, Pmc, PlayerScav, Goon, All
# Multiple values can be set at the same time by separating them with , (e.g. Debug, Warning)
Enable looting = Scav, Pmc, PlayerScav

## ON (default): a badly wounded PMC / PlayerScav with no usable meds left breaks off and extracts on its own instead of dying where it stands, and rejoins the squad if it heals back up. OFF: members only leave on the loot / time / squad triggers.
# Setting type: Boolean
# Default value: true
Emergency extract when wounded = true

## ON: squads far from every player re-decide much less often to save CPU. Movement/aiming/doors still run, they just defer NEW decisions. NOTE: the gain is small — ORBIT is a thin slice of a bot's cost (SAIN/EFT combat, vision, pathing dominate), so leaving this OFF (default) is fine for most setups.
# Setting type: Boolean
# Default value: false
Degraded tickrate for off-screen squads = false

## ON: the world gently drifts toward the human player(s), bringing more action your way. OFF (default): no pull. (Strength/range are under Advection in Advanced.)
# Setting type: Boolean
# Default value: false
Player convergence = false

[02. Factions]

## ON: bot scavs run BSG's vanilla brain instead of ORBIT (so 'Roaming Scavs' does nothing). OFF (default): ORBIT controls them. PlayerScavs always stay on ORBIT.
# Setting type: Boolean
# Default value: false
Vanilla scavs (RESTART) = false

## ON: Goons (Knight, Big Pipe, Bird Eye) run BSG's vanilla brain. OFF (default): ORBIT controls them.
# Setting type: Boolean
# Default value: false
Vanilla goons (RESTART) = false

## ON: Cultists run BSG's vanilla brain. OFF (default): ORBIT controls them.
# Setting type: Boolean
# Default value: false
Vanilla cultists (RESTART) = false

## ON (default): Raiders (Reserve / Labs) and Rogues (Lighthouse) run BSG's vanilla brain. OFF: ORBIT controls them.
# Setting type: Boolean
# Default value: true
Vanilla raiders (RESTART) = true

## OFF (default): scavs stay near their spawn area. ON: they roam the whole map like PMCs.
# Setting type: Boolean
# Default value: false
Roaming Scavs = false

## ON (default): Goons roam the whole map freely. OFF: ORBIT keeps nudging them back toward their spawn area whenever they're not fighting (a steady pull toward spawn) — but they still cover a lot of ground, since Goons hear and see from very far.
# Setting type: Boolean
# Default value: true
Roaming Goons = true

## ON: Bloodhounds (Smugglers / arena spawns) run BSG's vanilla brain. OFF (default): ORBIT controls them.
# Setting type: Boolean
# Default value: false
Vanilla bloodhounds (RESTART) = false

## OFF: Bloodhounds stay near their spawn. ON (default): they roam the whole map.
# Setting type: Boolean
# Default value: true
Roaming Bloodhounds = true

## ON: ORBIT routes UNTAR bots like PMCs. OFF (default): they run on their own 'Go Home' behaviour.
# Setting type: Boolean
# Default value: false
Take over UNTAR bots = false

## ON: ORBIT routes RUAF bots like PMCs. OFF (default): they run on their own 'Come Home' behaviour.
# Setting type: Boolean
# Default value: false
Take over RUAF bots = false

## ON: ORBIT routes Black Division bots like PMCs. OFF (default): they run on their own behaviour.
# Setting type: Boolean
# Default value: false
Take over Black Division bots = false

## ON (default): ORBIT routes ISB bots like PMCs. OFF: they run on their own behaviour.
# Setting type: Boolean
# Default value: true
Take over ISB bots = true

## ON: ORBIT routes Manimal's Combine Soldiers like PMCs. OFF (default): they run on their own behaviour.
# Setting type: Boolean
# Default value: false
Take over Combine Soldiers bots = false

[03. PlayerScav]

## OFF: PlayerScav squads skip the Quest / Kills / LootValue goal system.
# Setting type: Boolean
# Default value: true
Enabled (goal system) = true

## Fewest objectives a PlayerScav squad rolls.
# Setting type: Int32
# Default value: 1
# Acceptable value range: From 1 to 20
Main count min = 1

## Most objectives a PlayerScav squad rolls.
# Setting type: Int32
# Default value: 5
# Acceptable value range: From 1 to 20
Main count max = 5

## Share of Quest objectives in the PlayerScav mix (normalized against Kills + LootValue).
# Setting type: Single
# Default value: 0.1
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.1

## Share of Kills objectives in the PlayerScav mix.
# Setting type: Single
# Default value: 0.3
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.3

## Share of LootValue objectives in the PlayerScav mix.
# Setting type: Single
# Default value: 0.6
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.6

## Random window (as % of raid time left) when a PlayerScav squad decides to head out.
# Setting type: Vector2
# Default value: {"x":10.0,"y":30.0}
Time extract window (%) = {"x":10.0,"y":30.0}

## Once a PlayerScav squad has looted this much (₽ across living members), it heads for the nearest exfil. 0 = never.
# Setting type: Single
# Default value: 200000
Extract at loot value (₽) = 200000

[04. Looting]

## How far from the squad leader a loot spot (container / item / corpse) is still considered. 0 = no limit.
# Setting type: Single
# Default value: 80
Detect loot distance (m) = 80

## ON (default): a corpse is only looted if the squad saw it or made the kill, so bots don't magically know about bodies across the map.
# Setting type: Boolean
# Default value: true
Corpse requires LoS or squad kill = true

## Which factions ORBIT routes to an exfil. Gates EVERY extract — the squad loot/time triggers AND the solo emergency (wounded) and loot-threshold extracts. None = nobody extracts. Only PMC and PlayerScav have extract logic.
# Setting type: ExtractFaction
# Default value: All
# Acceptable values: None, Pmc, PlayerScav, All
# Multiple values can be set at the same time by separating them with , (e.g. Debug, Warning)
Extract allowed for = All

## When a PMC/PlayerScav's OWN loot crosses its OWN extract threshold, the chance it leaves ALONE while the squad keeps playing (rolled once per member). 0 = stay with squad, 100 = always leave. (Wounded emergency extract is separate, in Essentials.)
# Setting type: Int32
# Default value: 50
# Acceptable value range: From 0 to 100
Solo extract on own loot threshold (%) = 50

## Bot scavs (not PlayerScavs) skip the value gate and just roll this chance per item, like vanilla opportunistic scavs. PMCs/PlayerScavs unaffected.
# Setting type: Int32
# Default value: 30
# Acceptable value range: From 0 to 100
Scav: per-item loot chance (%) = 30

[05. Performance]

## Squads within this distance of any player always run at full rate. Beyond it, the throttle below kicks in.
# Setting type: Single
# Default value: 200
# Acceptable value range: From 0 to 1000
Full-rate distance (m) = 200

## How often a far/off-screen squad re-decides. Higher = more CPU saved, slower reactions far away. 5–10s is a good range.
# Setting type: Single
# Default value: 6
# Acceptable value range: From 0.5 to 30
Far decision interval (s) = 6

[06. POI guard (RESTART)]

## How long a squad holds a quest/cover spot before picking a new objective. Higher = more static map.
# Setting type: Vector2
# Default value: {"x":60.0,"y":180.0}
Base guard duration (s, min..max) = {"x":60.0,"y":180.0}

## Same, but for virtual patrol points (no real loot/quest). Kept short so bots keep moving.
# Setting type: Vector2
# Default value: {"x":3.5,"y":6.5}
Synthetic POI guard duration (s, min..max) = {"x":3.5,"y":6.5}

## Once the whole squad has arrived at a loot/quest spot, the guard wait is cut to this fraction (e.g. 0.2–0.5 = 20–50% of base).
# Setting type: Vector2
# Default value: {"x":0.20000000298023225,"y":0.5}
Loot/Quest guard duration cut (×, min..max) = {"x":0.20000000298023225,"y":0.5}

[07. Advection & convergence]

## Multiplier on per-map advection-zone radius. 1.0 = author defaults.
# Setting type: Single
# Default value: 1
# Acceptable value range: From 0 to 10
Zone radius scale = 1

## Multiplier on advection force. Negative flips attractors↔repulsors; 0 disables advection.
# Setting type: Single
# Default value: 1
# Acceptable value range: From -10 to 10
Zone force scale = 1

## How fast a zone's force fades with distance. Larger = tighter to the zone.
# Setting type: Single
# Default value: 1
# Acceptable value range: From 0 to 5
Zone falloff scale = 1

## Multiplier on how far the pull toward players reaches. 1.0 = author defaults.
# Setting type: Single
# Default value: 1
# Acceptable value range: From 0 to 10
Convergence radius scale = 1

## Multiplier on how strongly bots are pulled toward players. Negative pushes them AWAY; 0 disables.
# Setting type: Single
# Default value: 1
# Acceptable value range: From -10 to 10
Convergence force scale = 1

[08. Main objectives]

## Master switch for the goal system (Quest / Kills / LootValue objectives). OFF = plain dispatch.
# Setting type: Boolean
# Default value: true
Enabled = true

## OFF: PMC squads skip the goal system.
# Setting type: Boolean
# Default value: true
Enabled for PMC = true

## ON (default): the squad heads for an exfil once every objective is finished.
# Setting type: Boolean
# Default value: true
Extract when all mains done = true

## How hard squads commit to their objectives. Larger = more goal-focused.
# Setting type: Single
# Default value: 4
# Acceptable value range: From 0 to 20
Main pull strength = 4

## Pull toward a Kills objective while hunting around it.
# Setting type: Single
# Default value: 3
# Acceptable value range: From 0 to 20
Kills roam pull strength = 3

## Safety net: a LootValue objective auto-finishes after this many seconds of engaged time.
# Setting type: Single
# Default value: 300
# Acceptable value range: From 30 to 1800
LootValue timeout (s) = 300

## How long the rally-to-combat pull lingers after the last shots.
# Setting type: Single
# Default value: 5
# Acceptable value range: From 0 to 60
Combat caller grace (s) = 5

## How far each member spreads to pick its own spot while roaming a Kills / LootValue objective.
# Setting type: Single
# Default value: 50
# Acceptable value range: From 10 to 200
Roam splinter radius (m) = 50

## Loot within this vertical distance counts as 'same floor' and is preferred over loot on another floor (stops the staircase yo-yo). 0 = ignore floors.
# Setting type: Single
# Default value: 2.5
# Acceptable value range: From 0 to 10
Same-floor sweep tolerance (m) = 2.5

## Chance a bot picks loot on another floor instead of finishing the current one, so it drifts up/down naturally. 0% = clear a floor before moving; 100% = ignore floors.
# Setting type: Single
# Default value: 0.1
# Acceptable value range: From 0 to 1
Cross-floor splinter chance = 0.1

## Random window (as % of raid time left) when a PMC squad decides to head out. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":10.0,"y":30.0}
Time extract window — PMC (%) = {"x":10.0,"y":30.0}

## After looting a cell, how long before the same PMC squad will go back. Stops boomeranging. 0 = no cooldown.
# Setting type: Single
# Default value: 600
# Acceptable value range: From 0 to 3600
PMC loot cell cooldown (s) = 600

## After finishing a patrol point, how long before the same squad revisits it. 0 = no cooldown.
# Setting type: Single
# Default value: 180
# Acceptable value range: From 0 to 1800
Synthetic POI cooldown (s) = 180

## How often a squad checks for a fresh corpse nearby. Lower = snappier corpse looting but more CPU under heavy bot counts.
# Setting type: Single
# Default value: 2.5
# Acceptable value range: From 0.5 to 10
Opportunistic corpse scan (s) = 2.5

[09.0 SAIN personality - General]

## ON (default): PMC squads use the per-archetype values in 09.1–09.5 (matched from their SAIN brain) instead of the fallback knobs in section 05/06. Scavs/PlayerScavs always use the fallback.
# Setting type: Boolean
# Default value: true
Enable SAIN personality = true

## SAIN brain names (comma-separated, case-insensitive) treated as Timmy (random/erratic).
# Setting type: String
# Default value: Timmy
Brain names → Timmy (RESTART) = Timmy

## SAIN brain names treated as Cautious (low-risk, loot-focused).
# Setting type: String
# Default value: Rat, Coward, SnappingTurtle
Brain names → Cautious (RESTART) = Rat, Coward, SnappingTurtle

## SAIN brain names treated as Average (balanced). Any brain not listed in the five lists also falls back to Average.
# Setting type: String
# Default value: Normal
Brain names → Average (RESTART) = Normal

## SAIN brain names treated as Aggressive.
# Setting type: String
# Default value: Wreckless, Chad
Brain names → Aggressive (RESTART) = Wreckless, Chad

## SAIN brain names treated as Very aggressive.
# Setting type: String
# Default value: GigaChad
Brain names → Very aggressive (RESTART) = GigaChad

## ON: Timmy squads also get a 20% wrong-cell pick and 5% forget-blacklist quirk on top of their 09.1 numbers.
# Setting type: Boolean
# Default value: true
Timmy: erratic extras = true

[09.1 SAIN personality - Timmy]

## Share of Quest mains for this archetype (auto-normalized against Kills + LootValue, so they need not total exactly 100%).
# Setting type: Single
# Default value: 0.29
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.29

## Share of Kills mains for this archetype.
# Setting type: Single
# Default value: 0.29
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.29

## Share of LootValue mains for this archetype.
# Setting type: Single
# Default value: 0.42
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.42

## Number of mains rolled per squad, uniform in [min..max].
# Setting type: Vector2
# Default value: {"x":1.0,"y":2.0}
Main count (min, max) = {"x":1.0,"y":2.0}

## Rouble value that flips ExtractRequested. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":100000.0,"y":300000.0}
Extract loot threshold (₽, min..max) = {"x":100000.0,"y":300000.0}

## Per-POI pick probability (LootValue cell + scavenge sweep). 1.0 = vacuum, 0.3 = skips most.
# Setting type: Vector2
# Default value: {"x":0.30000001192092898,"y":0.5}
Loot coverage % (min..max) = {"x":0.30000001192092898,"y":0.5}

## 0 = never sprint, 1 = always.
# Setting type: Single
# Default value: 0
Sprint propensity (0..1) = 0

## Probability of force-unlocking a locked door for an intermediate POI. Main-anchor POIs always roll 100%.
# Setting type: Single
# Default value: 0.1
Locked door unlock % = 0.1

## Minimum item value the bot bothers picking up.
# Setting type: Int32
# Default value: 0
Mini-loot threshold (₽) = 0

## After a loot, chain to the nearest LooseLoot/Corpse within this radius.
# Setting type: Single
# Default value: 10
Scavenge sweep radius (m) = 10

## Non-leader members spread to splinter POIs within this radius of the leader.
# Setting type: Single
# Default value: 30
Follower splinter radius (m) = 30

## Time the squad spends roaming the Kills anchor before completing.
# Setting type: Vector2
# Default value: {"x":30.0,"y":150.0}
Kills roam duration (s, min..max) = {"x":30.0,"y":150.0}

## LootValue rolls are restricted to the top-N richest cells. Smaller = more concentration (more PvP).
# Setting type: Int32
# Default value: 10
Top loot cells max = 10

[09.2 SAIN personality - Cautious]

## Share of Quest mains for this archetype (auto-normalized against Kills + LootValue, so they need not total exactly 100%).
# Setting type: Single
# Default value: 0.23
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.23

## Share of Kills mains for this archetype.
# Setting type: Single
# Default value: 0.06
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.06

## Share of LootValue mains for this archetype.
# Setting type: Single
# Default value: 0.71
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.71

## Number of mains rolled per squad, uniform in [min..max].
# Setting type: Vector2
# Default value: {"x":2.0,"y":4.0}
Main count (min, max) = {"x":2.0,"y":4.0}

## Rouble value that flips ExtractRequested. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":200000.0,"y":500000.0}
Extract loot threshold (₽, min..max) = {"x":200000.0,"y":500000.0}

## Per-POI pick probability (LootValue cell + scavenge sweep). 1.0 = vacuum, 0.3 = skips most.
# Setting type: Vector2
# Default value: {"x":0.8500000238418579,"y":0.949999988079071}
Loot coverage % (min..max) = {"x":0.8500000238418579,"y":0.949999988079071}

## 0 = never sprint, 1 = always.
# Setting type: Single
# Default value: 0.2
Sprint propensity (0..1) = 0.2

## Probability of force-unlocking a locked door for an intermediate POI. Main-anchor POIs always roll 100%.
# Setting type: Single
# Default value: 0.1
Locked door unlock % = 0.1

## Minimum item value the bot bothers picking up.
# Setting type: Int32
# Default value: 5000
Mini-loot threshold (₽) = 5000

## After a loot, chain to the nearest LooseLoot/Corpse within this radius.
# Setting type: Single
# Default value: 15
Scavenge sweep radius (m) = 15

## Non-leader members spread to splinter POIs within this radius of the leader.
# Setting type: Single
# Default value: 18
Follower splinter radius (m) = 18

## Time the squad spends roaming the Kills anchor before completing.
# Setting type: Vector2
# Default value: {"x":30.0,"y":150.0}
Kills roam duration (s, min..max) = {"x":30.0,"y":150.0}

## LootValue rolls are restricted to the top-N richest cells. Smaller = more concentration (more PvP).
# Setting type: Int32
# Default value: 10
Top loot cells max = 10

[09.3 SAIN personality - Average]

## Share of Quest mains for this archetype (auto-normalized against Kills + LootValue, so they need not total exactly 100%).
# Setting type: Single
# Default value: 0.34
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.34

## Share of Kills mains for this archetype.
# Setting type: Single
# Default value: 0.33
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.33

## Share of LootValue mains for this archetype.
# Setting type: Single
# Default value: 0.33
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.33

## Number of mains rolled per squad, uniform in [min..max].
# Setting type: Vector2
# Default value: {"x":1.0,"y":5.0}
Main count (min, max) = {"x":1.0,"y":5.0}

## Rouble value that flips ExtractRequested. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":500000.0,"y":1000000.0}
Extract loot threshold (₽, min..max) = {"x":500000.0,"y":1000000.0}

## Per-POI pick probability (LootValue cell + scavenge sweep). 1.0 = vacuum, 0.3 = skips most.
# Setting type: Vector2
# Default value: {"x":0.6499999761581421,"y":0.75}
Loot coverage % (min..max) = {"x":0.6499999761581421,"y":0.75}

## 0 = never sprint, 1 = always.
# Setting type: Single
# Default value: 0.5
Sprint propensity (0..1) = 0.5

## Probability of force-unlocking a locked door for an intermediate POI. Main-anchor POIs always roll 100%.
# Setting type: Single
# Default value: 0.3
Locked door unlock % = 0.3

## Minimum item value the bot bothers picking up.
# Setting type: Int32
# Default value: 10000
Mini-loot threshold (₽) = 10000

## After a loot, chain to the nearest LooseLoot/Corpse within this radius.
# Setting type: Single
# Default value: 10
Scavenge sweep radius (m) = 10

## Non-leader members spread to splinter POIs within this radius of the leader.
# Setting type: Single
# Default value: 30
Follower splinter radius (m) = 30

## Time the squad spends roaming the Kills anchor before completing.
# Setting type: Vector2
# Default value: {"x":60.0,"y":300.0}
Kills roam duration (s, min..max) = {"x":60.0,"y":300.0}

## LootValue rolls are restricted to the top-N richest cells. Smaller = more concentration (more PvP).
# Setting type: Int32
# Default value: 10
Top loot cells max = 10

[09.4 SAIN personality - Aggressive]

## Share of Quest mains for this archetype (auto-normalized against Kills + LootValue, so they need not total exactly 100%).
# Setting type: Single
# Default value: 0.18
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.18

## Share of Kills mains for this archetype.
# Setting type: Single
# Default value: 0.64
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.64

## Share of LootValue mains for this archetype.
# Setting type: Single
# Default value: 0.18
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.18

## Number of mains rolled per squad, uniform in [min..max].
# Setting type: Vector2
# Default value: {"x":2.0,"y":4.0}
Main count (min, max) = {"x":2.0,"y":4.0}

## Rouble value that flips ExtractRequested. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":1000000.0,"y":1500000.0}
Extract loot threshold (₽, min..max) = {"x":1000000.0,"y":1500000.0}

## Per-POI pick probability (LootValue cell + scavenge sweep). 1.0 = vacuum, 0.3 = skips most.
# Setting type: Vector2
# Default value: {"x":0.5,"y":0.6000000238418579}
Loot coverage % (min..max) = {"x":0.5,"y":0.6000000238418579}

## 0 = never sprint, 1 = always.
# Setting type: Single
# Default value: 0.8
Sprint propensity (0..1) = 0.8

## Probability of force-unlocking a locked door for an intermediate POI. Main-anchor POIs always roll 100%.
# Setting type: Single
# Default value: 0.45
Locked door unlock % = 0.45

## Minimum item value the bot bothers picking up.
# Setting type: Int32
# Default value: 15000
Mini-loot threshold (₽) = 15000

## After a loot, chain to the nearest LooseLoot/Corpse within this radius.
# Setting type: Single
# Default value: 8
Scavenge sweep radius (m) = 8

## Non-leader members spread to splinter POIs within this radius of the leader.
# Setting type: Single
# Default value: 39
Follower splinter radius (m) = 39

## Time the squad spends roaming the Kills anchor before completing.
# Setting type: Vector2
# Default value: {"x":90.0,"y":450.0}
Kills roam duration (s, min..max) = {"x":90.0,"y":450.0}

## LootValue rolls are restricted to the top-N richest cells. Smaller = more concentration (more PvP).
# Setting type: Int32
# Default value: 5
Top loot cells max = 5

[09.5 SAIN personality - Very aggressive]

## Share of Quest mains for this archetype (auto-normalized against Kills + LootValue, so they need not total exactly 100%).
# Setting type: Single
# Default value: 0.06
# Acceptable value range: From 0 to 1
Main mix — Quest % = 0.06

## Share of Kills mains for this archetype.
# Setting type: Single
# Default value: 0.83
# Acceptable value range: From 0 to 1
Main mix — Kills % = 0.83

## Share of LootValue mains for this archetype.
# Setting type: Single
# Default value: 0.11
# Acceptable value range: From 0 to 1
Main mix — LootValue % = 0.11

## Number of mains rolled per squad, uniform in [min..max].
# Setting type: Vector2
# Default value: {"x":2.0,"y":5.0}
Main count (min, max) = {"x":2.0,"y":5.0}

## Rouble value that flips ExtractRequested. Rolled once per squad.
# Setting type: Vector2
# Default value: {"x":1500000.0,"y":3000000.0}
Extract loot threshold (₽, min..max) = {"x":1500000.0,"y":3000000.0}

## Per-POI pick probability (LootValue cell + scavenge sweep). 1.0 = vacuum, 0.3 = skips most.
# Setting type: Vector2
# Default value: {"x":0.30000001192092898,"y":0.44999998807907107}
Loot coverage % (min..max) = {"x":0.30000001192092898,"y":0.44999998807907107}

## 0 = never sprint, 1 = always.
# Setting type: Single
# Default value: 1
Sprint propensity (0..1) = 1

## Probability of force-unlocking a locked door for an intermediate POI. Main-anchor POIs always roll 100%.
# Setting type: Single
# Default value: 0.6
Locked door unlock % = 0.6

## Minimum item value the bot bothers picking up.
# Setting type: Int32
# Default value: 20000
Mini-loot threshold (₽) = 20000

## After a loot, chain to the nearest LooseLoot/Corpse within this radius.
# Setting type: Single
# Default value: 5
Scavenge sweep radius (m) = 5

## Non-leader members spread to splinter POIs within this radius of the leader.
# Setting type: Single
# Default value: 45
Follower splinter radius (m) = 45

## Time the squad spends roaming the Kills anchor before completing.
# Setting type: Vector2
# Default value: {"x":150.0,"y":750.0}
Kills roam duration (s, min..max) = {"x":150.0,"y":750.0}

## LootValue rolls are restricted to the top-N richest cells. Smaller = more concentration (more PvP).
# Setting type: Int32
# Default value: 3
Top loot cells max = 3

